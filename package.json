{
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun install --cwd Site && cd Site && bun run build",
    "dev": "bun install --cwd Site && cd Site && bun run dev",
    "preview": "bun install --cwd Site && cd Site && bun run preview"
  }
}
